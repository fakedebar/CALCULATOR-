A simple C++ calculator program that performs basic arithmetic operations (+, -, *, /). The program validates the user input and stops if an invalid operation is entered, displaying an error message.
